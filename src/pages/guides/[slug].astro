---
/**
 * Historical dynamic guide generation is intentionally disabled.
 *
 * Public research is limited to the four reviewed static sibling routes:
 * - /guides/ai-agent-reliability-audit
 * - /guides/security-sandboxing
 * - /guides/fleet-coordinator-drift-monitoring
 * - /guides/d3-tool-io-bridge-contract
 *
 * vercel.json redirects every other /guides/:slug request to /guides.
 * Returning an empty static-path set keeps legacy generated content out of
 * the physical static build while preserving the explicit quarantine route.
 */
export function getStaticPaths() {
  return [];
}
---

<!-- Intentionally no generated dynamic guide pages. -->
